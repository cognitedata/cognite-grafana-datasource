; This project resolves everything from the public registry (yarn.lock is pinned to
; registry.yarnpkg.com). Pin it here so a machine-level registry override (e.g. a
; corporate Artifactory mirror in ~/.npmrc) does not break or drift installs.
registry=https://registry.yarnpkg.com/
