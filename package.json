{
  "name": "cognite-cdp-datasource",
  "version": "0.0.1",
  "description": "Cognite Data Platform datasource",
  "repository": "https://github.com/cognitedata/cognite-dataplatform-datasource",
  "author": "Cognite AS",
  "license": "Apache-2.0",
  "scripts": {
    "build": "rm -rf dist/* && cp src/plugin.json dist/ && cp -r src/css dist/ && cp -r src/partials dist/ && npx tsc -m system --outDir dist/ src/*ts"
  },
  "devDependencies": {
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "lodash": "^4.17.10",
    "q": "^1.5.1"
  }
}
