{
  "name": "@cognite/cognite-grafana-datasource",
  "version": "0.0.1",
  "description": "Cognite Data Platform datasource",
  "repository": "https://github.com/cognitedata/cognite-grafana-datasource",
  "author": "Cognite AS",
  "license": "Apache-2.0",
  "scripts": {
    "build": "rm -rf dist && npx tsc && cp -r src/plugin.json src/css src/partials src/img dist/",
    "run-docker": "docker build -t cognite-grafana . && docker run -p 3000:3000 cognite-grafana"
  },
  "devDependencies": {
    "@types/react": "^16.7.22",
    "husky": "^1.3.1",
    "lint-staged": "^8.1.1",
    "prettier": "1.16.3",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "lodash": "^4.17.10",
    "q": "^1.5.1",
    "react": "^16.7.0"
  },
  "husky": {
    "hooks": {
      "pre-commit": "lint-staged"
    }
  },
  "lint-staged": {
    "*.{ts,tsx,js,json,css,md}": [
      "prettier --write",
      "git add"
    ]
  }
}
